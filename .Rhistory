Sys.getenv("EIA_API_KEY")
api_key<-Sys.getenv("EIA_API_KEY")
api_key
install.packages(c("EIAapi", "dplyr", "lubridate", "plotly"))
library(EIA)
library(EIAapi)
source("~/Work/data-pipeline-automation-eia/R/r_eia_api.qmd.R")
# PULLING METADATA FROM API
# Setting the api key and the api path to pull data:
api_key <- Sys.getenv("EIA_API_KEY")
api_meta_path <- "electricity/rto/region-sub-ba-data"
# Sending GET request for route metadata (data descriptions):
eia_metadata(api_key = api_key,
api_path = api_meta_path)
setwd("~/Work/data-pipeline-automation-eia")
usethis::edit_r_environ()
install.packages("usethis")
# Sending GET request for route metadata (data descriptions):
eia_metadata(api_key = api_key,
api_path = api_meta_path)
# Sending GET request for route metadata (data descriptions):
eia_metadata(api_key = "cqFh0xC1OXmMgdJzbDzOmmVmSAldFgiqshB1VDp6",
api_path = api_meta_path)
# PULLING METADATA FROM API
# Setting the api key and the api path to pull data:
api_key <- Sys.getenv("EIA_API_KEY")
api_key
# PULLING METADATA FROM API
# Setting the api key and the api path to pull data:
Sys.getenv(EIA_API_KEY='EIA_API_KEY')
# PULLING METADATA FROM API
# Setting the api key and the api path to pull data:
api_key <- Sys.getenv("EIA_API_KEY")
api_meta_path <- "electricity/rto/region-sub-ba-data"
# Sending GET request for route metadata (data descriptions):
eia_metadata(api_key = api_key,
api_path = api_meta_path)
api_key
api_key
# LIBRARIES
library(EIAapi)
library(dplyr)
library(lubridate)
library(plotly)
# PULLING METADATA FROM API
# Setting the api key and the api path to pull data:
api_key <- Sys.getenv("EIA_API_KEY")
api_meta_path <- "electricity/rto/region-sub-ba-data"
# Sending GET request for route metadata (data descriptions):
eia_metadata(api_key = api_key,
api_path = api_meta_path)
